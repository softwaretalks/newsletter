name: "Send email for test users"

on:
  workflow_dispatch:

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          tools: composer:v2

      - name: Install Composer dependencies
        run: composer install --no-progress --optimize-autoloader

      - name: Run script
        run: php main.php
        env:
          PAKAT_SMTP_USERNAME: ${{ secrets.PAKAT_SMTP_USERNAME }}
          PAKAT_SMTP_PASSWORD: ${{ secrets.PAKAT_SMTP_PASSWORD }}
          PAKAT_API_KEY:  ${{ secrets.PAKAT_API_KEY }}
          SEND_ENV: 'test'
          # BOTTOM_CONTENT_HTML: '<p style="font-size: 16px; text-align: center; font-family: 'Samim'; direction: rtl; color: #263238; "><img src='https://pakat.net/wp-content/uploads/2020/02/pakat-new-logo-white_00ea005b1_2512.png'><br> ارسال شده توسط <a href="https://pakat.net/">پاکت</a></p><p style=" font-size: 14px; text-align: center; font-family: 'Samim'; direction: rtl; color: #263238; "> <a href="https://pakat.net/">سرویس پاکت</a> اسپانسر ارسال ایمیل<200c>های این خبرنامه است. با کد تخفیف sota10 می<200c>توانید از ۱۰ درصد تخفیف در خرید پلن<200c>های پاکت بهره<200c>مند شوید</p>'

